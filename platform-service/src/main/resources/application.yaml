spring:
  application:
    name: categories-service
  datasource:
    url: ${DB_HOST:jdbc:mysql://localhost:3306/authDB?createDatabaseIfNotExist=true&useUnicode=true&serverTimezone=UTC}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Qwerty-2}
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        show_sql: true
        format_sql: true
server:
  port: ${PORT:8081}

